### 1. Cài đặt gnu c++11 để sử dụng nhiều tính năng hơn thay vì sử dụng gnu c99:

`Tools` $->$ `Compiler Options...` $->$ `General` $->$ `Add the following commands...: -static-libgcc` $->$ `Setting` $->$ `Code Generation` $->$ `Language standard (-std): GNU C++11` 
